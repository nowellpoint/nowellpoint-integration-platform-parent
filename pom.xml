<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>com.nowellpoint</groupId>
	<artifactId>aws</artifactId>
	<packaging>pom</packaging>
	<version>0.0.2-SNAPSHOT</version>
	<name>Nowellpoint Integration Platform Parent</name>

	<parent>
		<groupId>org.sonatype.oss</groupId>
		<artifactId>oss-parent</artifactId>
		<version>7</version>
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
		<aws.sdk.version>1.10.58</aws.sdk.version>
		<jackson.version>2.7.0</jackson.version>
		<hibernate-validator-version>5.2.2.Final</hibernate-validator-version>
		<javax.el.version>2.2.4</javax.el.version>
		<mongodb.driver.version>3.2.0</mongodb.driver.version>
		<aws.dynamodb.encryption.version>0.0.5-SNAPSHOT</aws.dynamodb.encryption.version>
		<jedis.version>2.8.0</jedis.version>
		<junit.version>4.12</junit.version>
	</properties>

	<modules>
		<module>nowellpoint-aws-core</module>	
		<module>nowellpoint-aws-data</module>
		<module>nowellpoint-aws-idp</module>
		<module>nowellpoint-aws-idp-impl</module>
		<module>nowellpoint-aws-sforce</module>
		<module>nowellpoint-aws-event</module>
		<!-- <module>nowellpoint-aws-util</module>  -->
		<module>nowellpoint-aws-admin</module>
		<module>nowellpoint-aws-api</module>
        <module>nowellpoint-www-app</module>
		<module>nowellpoint-aws-test</module>
	</modules>

</project>
